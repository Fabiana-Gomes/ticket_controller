function redirect(link) {
    console.log('chegou na função')
    window.location.href = link;
}

// Pode adicionar estas funções se quiser mantê-las em arquivo separado
function openModal(ticket) {
    // Mesma implementação do código acima
}

function closeModal() {
    // Mesma implementação do código acima
}

// Ou pode deixar tudo no próprio index.php como no exemplo